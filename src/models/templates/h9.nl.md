+++
title = "Model H 9 - Instemmingsverklaring"
language = "nl"
header_right = "Model H 9 - Instemmingsverklaring"
footer_right = "Pagina {page} van {total}"
+++

## Model H 9

# Instemmingsverklaring

Met dit formulier stemt u ermee in dat u op onderstaande kandidatenlijst staat,
en u stemt in met uw positie op die lijst.

{ background = "highlight" }
> **Let op!**\
> Bent u nog geen lid van het vertegenwoordigend orgaan? Voeg dan een kopie van
> een geldig identiteitsbewijs bij.

### Verkiezing

Het gaat om de verkiezing van **{{ common.election_name }}**

{# This wording slightly differs from the reference H 9 but is confirmed to be
   legal. Single-district elections omit the section. #}
{% match electoral_districts %}
{% when ElectoralDistricts::All %}
### Kieskringen

Mijn instemming geldt voor: **alle kieskringen**

{% when ElectoralDistricts::Some(names) %}
### Kieskringen

Mijn instemming geldt voor:

{{ names.join(", ") }}

{% when ElectoralDistricts::OnlyOne %}
{% endmatch %}
### Politieke groepering

De aanduiding van de politieke groepering waarvan de kandidatenlijst is: {% if !common.appellation.is_empty() %}**{{ common.appellation }}**{% endif %}

### Kandidaten op de lijst

{ widths = "auto 1 1 1" }
|  | naam | voorletters | woonplaats |
| --- | --- | --- | --- |
{%- for c in common.candidates %}
| {{ c.position }} | {{ c.last_name|cell }} | {{ c.initials|cell }} | {{ c.locality|cell }} |
{%- endfor %}

{% if common.election_type == ElectionType::Kcni %}
{# a postal address does not apply to the election of the electoral college for non-residents #}
### Kennisgeving van mijn benoeming ontvangen langs digitale weg

- [ ] Ik stem ermee in de kennisgeving van mijn benoeming te ontvangen via een
  digitale berichtenbox waartoe ik toegang kan krijgen met gebruikmaking van een
  DigiD. Hierbij bevestig ik tevens dat ik een DigiD zal aanvragen indien ik hier
  nog niet over beschik.
{% else if detailed_candidate.needs_representative %}
### Gemachtigde voor het aannemen van uw benoeming

{ widths = "4 4 4 auto 6" }
| naam | voorletters | postadres | postcode | plaats |
| --- | --- | --- | --- | --- |
{%- if let Some(representative) = detailed_candidate.representative %}
| {{ representative.last_name|cell }} | {{ representative.initials|cell }} | {{ representative.postal_address.street_address|cell }} | {{ representative.postal_address.postal_code|mono_cell }} | {{ representative.postal_address.locality|cell }} |
{% else %}
|  |  |  |  |  |
{% endif %}
{% else %}
### Adres voor de kennisgeving van mijn benoeming

{% if let Some(address) = detailed_candidate.postal_address %}
{ widths = "1 auto 1" }
| postadres | postcode | plaats |
| --- | --- | --- |
| {{ address.street_address|cell }} | {{ address.postal_code|mono_cell }} | {{ address.locality|cell }} |
{% else %}
*niet van toepassing*
{% endif %}
{% endif %}

### Ondertekening door de kandidaat

| Naam | {{ detailed_candidate.candidate.last_name|cell }}, {{ detailed_candidate.initials_no_gender|cell }} |
| Woonplaats | {{ detailed_candidate.candidate.locality|cell }} |
| Burgerservicenummer | {{ detailed_candidate.bsn|display|cell }} |
| Datum | ________ |

{ row-height = "3.5em" }
| Handtekening | ________ |
